import 'package:flutter/material.dart';

class ImageDetectionHomeScreen extends StatelessWidget {
  const ImageDetectionHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
